<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <%= form_for(@school_class) do |f| %>
    <%= f.text_field :title %>
    <%= f.text_field :room_number %>
    <%= f.submit "Update School class" %>
    <% end %>

  </body>
</html>
